services:
  - type: web
    name: embedbase-render
    env: python
    rootDir: embedbase
    plan: free
    buildCommand: pip install embedbase sentence-transformers
    startCommand: "embedbase run"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.11
